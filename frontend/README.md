# Frontend

Interface do dashboard em `Next.js + React + Tailwind + TypeScript`.

## Rodar local

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Docker

O Docker e orquestrado pelo `docker-compose.yml` da raiz do projeto.
